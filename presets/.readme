You can put in this folder LIS programs, with the '.lis' extension.
These files can be loaded using `Lisa.Script.download('presets://<program>[.lis]')`.

Example:
========

Lisa.Script.download('presets://HelloWorld')
OR Lisa.Script.download('presets://HelloWorld.lis')

  -> Loads the 'presets/HelloWorld.lis' program, compile it and run the
     built JavaScript code
